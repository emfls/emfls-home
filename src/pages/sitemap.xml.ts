import type { APIRoute } from 'astro'; import { site, categories } from '../data/site'; import { guides } from '../data/guides';
export const GET: APIRoute = ({ site: astroSite }) => { const urls=['/','/about/','/privacy/','/contact/','/editorial-policy/',...categories.map(c=>`/category/${c.slug}/`),...guides.map(g=>`/guides/${g.slug}/`)]; const body=urls.map(path=>`<url><loc>${new URL(path, astroSite ?? site.url).href.replace(/&/g,'&amp;')}</loc></url>`).join(''); return new Response(`<?xml version="1.0" encoding="UTF-8"?><urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">${body}</urlset>`,{headers:{'Content-Type':'application/xml; charset=utf-8'}}); };
